Start date: Saturday, May 9th
** Next
- refactor all the choices to the core app

**notes

EMAIL_PORT=587
EMAIL_PORT = env.int("EMAIL_PORT")

EMAIL_USE_TLS=True
EMAIL_USE_TLS = env.bool("EMAIL_USE_TLS")

PostgresSQL Example
DATABASE_URL=postgres://postgres:password@localhost:5432/mydb
DATABASES = {
    "default": env.db()
}


- add .env
- add .env, virtual env to .gitignore
**done

**class Next

1.  Divide PatientProfile models into separate models

Do NOT overload PatientProfile.

Your ePRO system should separate:

patient demographics
symptom reports
sypmtoms
symptom entry
chemotherapy sessions
appointments
vitals
lab results
PRO questionnaires

into separate models.

This model should remain primarily:

demographic
baseline clinical profile

That is good database normalization.

Your model design is already strong for an oncology ePRO MVP. 
The biggest future improvement is separating chemotherapy administration, symptom reporting, 
and patient outcomes into dedicated models rather than expanding this one model too much.

** recommended apps
App	            Responsibility
core            utils, 
users	        Login, authentication, permissions
patients	    Patient demographics/profile
clinicians	    Doctor/nurse profiles
chemotherapy	Regimens, cycles, administrations
symptoms	    ePROs, symptom entries, CTCAE
appointments	Scheduling
telemedicine	Video consultations, audio, chat
notifications	SMS/email/in-app alerts
medical_records lab results, radiology reports

**Model Type	            Recommended ID
PatientProfile	            UUID
ClinicianProfile	        UUID
Appointment	                UUID
SymptomEntry	            UUID
ChemotherapyCycle	        UUID
Notification	            UUID
AuditLog	                UUID
Lookup tables	            Integer
Choice/reference models     Integer


Thesis Topic = 

"Effect of a Mobile-Based Symptom Monitoring System on 
the Severity of Chemotherapy-Induced Side Effects and 
Quality of Life Among Adult Patients Receiving Chemotherapy 
in a Tertiary Hospital in Nigeria"

## TO-DO 

Add BMI/Weight Tracking Later

ePRO systems often track:

weight
height
BMI
performance status

Especially in oncology.